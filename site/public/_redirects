# Serve o HTML com as OG tags do IESB para a rota da calculadora
/calculadora    /calculadora.html    200

# PDF de referências (força o arquivo estático, não passa pelo SPA)
/referencias    /referencias.pdf     200

# Fallback SPA para todas as outras rotas
/*              /index.html          200
